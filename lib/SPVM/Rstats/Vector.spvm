use Math;

package Rstats::Vector {
  
  sub sin($values_in : double[]) : double[] {
    
    my $values_out = new double[len $values_in];
    
    for (my $i = 0; $i < @$values_in; $i++) {
      $values_out->[$i] = Math::sin($values_in->[$i]);
    }
    
    return $values_out;
  }
}
