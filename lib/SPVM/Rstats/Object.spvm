package Rstats::Object {
  has type : int;
  has integral_values : long[];
  has double_values : double[];
  has complex_values : double[][];
  has string_values : byte[][];
  has dimension : int[];
  
  enum {
    TYPE_BOOLEAN,
    TYPE_INTEGER,
    TYPE_DOUBLE,
    TYPE_COMPLEX,
    TYPE_STRING,
  }
}
